# Desktop interface boundary

Reserved for Tauri or Electron after the authenticated API contract is established.
This is not a runnable desktop application. It will use the API; it must never
own canonical memory, database credentials or model-provider state.
