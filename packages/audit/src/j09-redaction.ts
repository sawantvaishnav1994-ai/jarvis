import { DataClassSchema } from "@jarvis/shared";
const secretKey=/(password|secret|token|cookie|authorization|api[_-]?key|private[_-]?key|credential|session[_-]?secret|encryption[_-]?key)/i,control=/[\u0000-\u001f\u007f]/g,reference=/^[A-Za-z0-9._:/@-]+$/;const sensitiveStringKey=/(?:id|ref)$/i.test.bind(/(?:id|ref)$/i);const boundedCategory=new Set(["status","result","classification","operation","subsystem","providerClass","toolClass","eventType","risk","previousState","newState"]);
export function safeText(value:string,max=256){return value.replace(control," ").slice(0,max);}
export function sanitizeMetadata(input:Record<string,unknown>,classification:string){const dataClass=DataClassSchema.parse(classification);if(dataClass==="D5")throw new Error("D5_AUDIT_CONTENT_DENIED");const output:Record<string,string|number|boolean|null>={},redactions:string[]=[];for(const[rawKey,raw]of Object.entries(input).slice(0,32)){const key=safeText(rawKey,64);if(secretKey.test(key)){redactions.push(key);continue;}if(raw===null||typeof raw==="boolean"||(typeof raw==="number"&&Number.isFinite(raw))){output[key]=raw;continue;}if(typeof raw==="string"){const value=safeText(raw);if(["D2","D3","D4"].includes(dataClass)&&!(boundedCategory.has(key)||sensitiveStringKey(key))){redactions.push(key);continue;}if(["D2","D3","D4"].includes(dataClass)&&!reference.test(value)){redactions.push(key);continue;}output[key]=value;continue;}redactions.push(key);}return{metadata:output,redactions};}
export function assertNoSecretLikeMaterial(value:unknown){const text=JSON.stringify(value);if(/Bearer\s+[A-Za-z0-9._-]+|-----BEGIN .*PRIVATE KEY-----|"(?:password|secret|token|cookie|apiKey|api_key)"\s*:\s*"(?!\[REDACTED\])/i.test(text))throw new Error("SECRET_MATERIAL_DENIED");}
