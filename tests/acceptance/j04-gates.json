{
  "version": 1,
  "A": ["accepts reviewed append-only baseline SQL", "applies migrations idempotently and keeps pgvector available", "A: creates a separately bounded extended-acceptance actor without resetting existing budgets"],
  "B": ["B-G-K: reconstructs provider-independent conversation, graph, settings and safe definitions with deletion metadata"],
  "C": ["B-D: persists canonical encrypted conversations and rejects cross-zone reads"],
  "D": ["B-D: persists canonical encrypted conversations and rejects cross-zone reads", "rejects modified transient payloads and cross-owner writes"],
  "E": ["E: encrypts object bytes and verifies the decrypted hash", "E-L: shared canonical objects survive one unlink, legacy links reconcile, final unlink permits purge", "E: encrypted objects keep canonical identity when the storage adapter destination changes"],
  "F": ["F-G: persists provenance, private vector linkage and knowledge lineage"],
  "G": ["F-G: persists provenance, private vector linkage and knowledge lineage", "B-G-K: reconstructs provider-independent conversation, graph, settings and safe definitions with deletion metadata"],
  "H": ["H: exact owner-approved tool consumes a vault handle without returning or auditing plaintext", "H: different handles, revoked authorizations and modified execution inputs cannot resolve secrets"],
  "I": ["I: rotates K1 to K2 and retains readable protected content"],
  "J": ["J: NEVER_STORE leaves no record, derived data or plaintext security history"],
  "K": ["K: exports portable records and objects with validated checksums, without secrets", "B-G-K: reconstructs provider-independent conversation, graph, settings and safe definitions with deletion metadata"],
  "L": ["L: forget removes memory, private embedding and derived entity, retaining minimal tombstones", "retention: exact expired cleanup purges derived vectors, rejects altered plans and replay", "L-N: deletion links recovery obligations and expired snapshots cannot restore", "E-L: shared canonical objects survive one unlink, legacy links reconcile, final unlink permits purge"],
  "M": ["M-N: validates encrypted backup and restores into a named isolated database without old sessions"],
  "N": ["M-N: validates encrypted backup and restores into a named isolated database without old sessions", "L-N: deletion links recovery obligations and expired snapshots cannot restore"],
  "O": ["O: rejects a corrupted backup before changing restored state"],
  "P": ["P: reusable migration registry requires exact current recovery evidence and one-time owner approval", "P: stale, corrupt and changed-source backups cannot authorize a destructive migration", "P: a pending unapproved migration cannot obtain execution authorization", "hashes all reviewed migration statements and rejects request-supplied SQL", "cannot execute a destructive migration outside the authenticated storage transaction"],
  "Q": ["Q: mixed external context excludes D5, local/private fields and wrong providers before synthetic invocation", "Q: an unsatisfied provider policy denies invocation rather than sending unfiltered fallback", "Q: authorized mixed-class retrieval minimizes before a synthetic provider and audits policy counts"],
  "R": ["R: schema, vector, vault and object failures deny protected operations and recover", "health reads never initialize missing key metadata"],
  "S": ["S: complete storage history includes safe recovery, secret, retention and context evidence", "S: data access history is append-only and payload-free; authorization cannot be replayed"]
}
